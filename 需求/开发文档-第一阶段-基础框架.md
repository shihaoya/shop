# 第一阶段开发文档 - 基础框架搭建

## 阶段概述
**目标**：完成前后端项目初始化、数据库设计、用户认证与授权模块  
**预计工期**：5-7天  
**优先级**：P0（最高）

---

## 一、技术栈选型

### 1.1 后端技术栈
- **运行环境**：Node.js 18+ LTS
- **Web框架**：Express.js 或 Koa2
- **ORM框架**：Sequelize 或 TypeORM
- **数据库**：MySQL 8.0+
- **认证**：JWT (jsonwebtoken)
- **密码加密**：bcryptjs
- **参数校验**：Joi 或 express-validator
- **日志**：winston
- **跨域**：cors

### 1.2 前端技术栈（PC端）
- **框架**：Vue 3 + Vite 或 React 18 + Vite
- **UI组件库**：Element Plus (Vue) 或 Ant Design (React)
- **状态管理**：Pinia (Vue) 或 Redux Toolkit (React)
- **路由**：Vue Router 或 React Router
- **HTTP客户端**：Axios
- **工具库**：lodash, dayjs

### 1.3 开发工具
- **包管理**：npm 或 yarn
- **代码规范**：ESLint + Prettier
- **Git工作流**：Git Flow

---

## 二、项目结构设计

### 2.1 后端项目结构
```
backend/
├── src/
│   ├── config/           # 配置文件
│   │   ├── database.js   # 数据库配置
│   │   ├── jwt.js        # JWT配置
│   │   └── index.js      # 配置入口
│   ├── models/           # 数据模型
│   │   ├── User.js
│   │   ├── Tenant.js
│   │   ├── UserRole.js
│   │   └── index.js
│   ├── controllers/      # 控制器
│   │   ├── authController.js
│   │   ├── userController.js
│   │   └── adminController.js
│   ├── services/         # 业务逻辑层
│   │   ├── authService.js
│   │   ├── userService.js
│   │   └── tenantService.js
│   ├── middlewares/      # 中间件
│   │   ├── auth.js       # 认证中间件
│   │   ├── permission.js # 权限中间件
│   │   ├── validator.js  # 验证中间件
│   │   └── logger.js     # 日志中间件
│   ├── routes/           # 路由
│   │   ├── auth.js
│   │   ├── user.js
│   │   ├── admin.js
│   │   └── index.js
│   ├── utils/            # 工具函数
│   │   ├── response.js   # 统一响应格式
│   │   ├── encrypt.js    # 加密工具
│   │   └── validator.js  # 验证工具
│   ├── logs/             # 日志文件
│   └── app.js            # 应用入口
├── .env                  # 环境变量
├── .gitignore
├── package.json
└── README.md
```

### 2.2 前端项目结构（PC端）
```
frontend-pc/
├── src/
│   ├── api/              # API接口
│   │   ├── auth.js
│   │   ├── user.js
│   │   └── index.js
│   ├── assets/           # 静态资源
│   │   ├── styles/
│   │   └── images/
│   ├── components/       # 公共组件
│   │   ├── Layout/
│   │   ├── Header/
│   │   ├── Sidebar/
│   │   └── LoginForm/
│   ├── views/            # 页面视图
│   │   ├── Login.vue
│   │   ├── Register.vue
│   │   ├── Admin/
│   │   │   ├── Dashboard.vue
│   │   │   ├── UserManage.vue
│   │   │   └── TenantAudit.vue
│   │   ├── Operator/
│   │   └ ├── User/
│   ├── router/           # 路由配置
│   │   └── index.js
│   ├── store/            # 状态管理
│   │   ├── user.js
│   │   └── index.js
│   ├── utils/            # 工具函数
│   │   ├── request.js    # axios封装
│   │   ├── auth.js       # 认证工具
│   │   └── storage.js    # 存储工具
│   ├── App.vue
│   └── main.js
├── .env                  # 环境变量
├── .gitignore
├── package.json
├── vite.config.js
└── README.md
```

---

## 三、数据库设计

### 3.1 数据库初始化

#### 创建数据库
```sql
CREATE DATABASE IF NOT EXISTS point_exchange_system 
DEFAULT CHARACTER SET utf8mb4 
DEFAULT COLLATE utf8mb4_unicode_ci;

USE point_exchange_system;
```

### 3.2 核心表结构

#### 用户表 (users)
```sql
CREATE TABLE `users` (
  `id` INT UNSIGNED AUTO_INCREMENT PRIMARY KEY COMMENT '用户ID',
  `username` VARCHAR(50) NOT NULL UNIQUE COMMENT '用户名',
  `password` VARCHAR(255) NOT NULL COMMENT '密码（bcrypt加密）',
  `nickname` VARCHAR(50) DEFAULT NULL COMMENT '昵称',
  `role` ENUM('admin', 'operator', 'user') NOT NULL DEFAULT 'user' COMMENT '角色',
  `status` TINYINT NOT NULL DEFAULT 1 COMMENT '状态：0-禁用，1-启用',
  `is_deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '逻辑删除：0-未删除，1-已删除',
  `deleted_at` DATETIME DEFAULT NULL COMMENT '删除时间',
  `last_login_at` DATETIME DEFAULT NULL COMMENT '最后登录时间',
  `login_fail_count` INT NOT NULL DEFAULT 0 COMMENT '登录失败次数',
  `locked_until` DATETIME DEFAULT NULL COMMENT '锁定到期时间',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  INDEX `idx_username` (`username`),
  INDEX `idx_role` (`role`),
  INDEX `idx_status` (`status`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户表';
```

#### 租户表 (tenants)
```sql
CREATE TABLE `tenants` (
  `id` INT UNSIGNED AUTO_INCREMENT PRIMARY KEY COMMENT '租户ID',
  `user_id` INT UNSIGNED NOT NULL COMMENT '关联的用户ID（运营方）',
  `name` VARCHAR(100) NOT NULL COMMENT '租户名称',
  `description` TEXT DEFAULT NULL COMMENT '申请描述',
  `status` ENUM('pending', 'approved', 'rejected', 'disabled') NOT NULL DEFAULT 'pending' COMMENT '状态',
  `reject_reason` TEXT DEFAULT NULL COMMENT '拒绝原因',
  `is_deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '逻辑删除',
  `deleted_at` DATETIME DEFAULT NULL COMMENT '删除时间',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  FOREIGN KEY (`user_id`) REFERENCES `users`(`id`) ON DELETE CASCADE,
  INDEX `idx_user_id` (`user_id`),
  INDEX `idx_status` (`status`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='租户表';
```

#### 用户-租户关联表 (user_tenant_relations)
```sql
CREATE TABLE `user_tenant_relations` (
  `id` INT UNSIGNED AUTO_INCREMENT PRIMARY KEY COMMENT '关联ID',
  `user_id` INT UNSIGNED NOT NULL COMMENT '用户ID',
  `tenant_id` INT UNSIGNED NOT NULL COMMENT '租户ID',
  `status` ENUM('pending', 'approved', 'rejected') NOT NULL DEFAULT 'pending' COMMENT '申请状态',
  `points_balance` INT NOT NULL DEFAULT 0 COMMENT '在该租户下的积分余额',
  `is_deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '逻辑删除',
  `deleted_at` DATETIME DEFAULT NULL COMMENT '删除时间',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  FOREIGN KEY (`user_id`) REFERENCES `users`(`id`) ON DELETE CASCADE,
  FOREIGN KEY (`tenant_id`) REFERENCES `tenants`(`id`) ON DELETE CASCADE,
  UNIQUE KEY `uk_user_tenant` (`user_id`, `tenant_id`),
  INDEX `idx_user_id` (`user_id`),
  INDEX `idx_tenant_id` (`tenant_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户-租户关联表';
```

#### 操作日志表 (operation_logs)
```sql
CREATE TABLE `operation_logs` (
  `id` BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY COMMENT '日志ID',
  `user_id` INT UNSIGNED DEFAULT NULL COMMENT '操作用户ID',
  `username` VARCHAR(50) DEFAULT NULL COMMENT '操作用户名',
  `operation_type` VARCHAR(50) NOT NULL COMMENT '操作类型',
  `operation_module` VARCHAR(50) NOT NULL COMMENT '操作模块',
  `operation_desc` TEXT NOT NULL COMMENT '操作描述',
  `request_method` VARCHAR(10) DEFAULT NULL COMMENT '请求方法',
  `request_url` VARCHAR(255) DEFAULT NULL COMMENT '请求URL',
  `request_params` TEXT DEFAULT NULL COMMENT '请求参数（JSON）',
  `response_code` INT DEFAULT NULL COMMENT '响应码',
  `ip_address` VARCHAR(50) DEFAULT NULL COMMENT 'IP地址',
  `user_agent` VARCHAR(255) DEFAULT NULL COMMENT '用户代理',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '操作时间',
  INDEX `idx_user_id` (`user_id`),
  INDEX `idx_operation_type` (`operation_type`),
  INDEX `idx_created_at` (`created_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='操作日志表';
```

### 3.3 初始化数据

#### 创建默认管理员账号
```sql
-- 密码: Admin@123456 (需要在应用中用bcrypt加密后插入)
INSERT INTO `users` (`username`, `password`, `nickname`, `role`, `status`) 
VALUES ('admin', '$2b$10$...', '系统管理员', 'admin', 1);
```

---

## 四、后端开发任务

### 4.1 基础配置

#### 任务1：项目初始化
- [ ] 初始化Node.js项目
- [ ] 安装依赖包
- [ ] 配置.env环境变量
- [ ] 配置ESLint和Prettier

#### 任务2：数据库连接
- [ ] 配置MySQL连接
- [ ] 创建Sequelize/TypeORM实例
- [ ] 测试数据库连接
- [ ] 执行数据库迁移脚本

#### 任务3：基础中间件
- [ ] CORS跨域配置
- [ ] JSON解析中间件
- [ ] 日志记录中间件
- [ ] 错误处理中间件
- [ ] 统一响应格式中间件

### 4.2 认证模块

#### 任务4：用户注册
**接口**：`POST /api/v1/auth/register`

**请求参数**：
```json
{
  "username": "string (必填, 3-50字符)",
  "password": "string (必填, 最少8位)",
  "nickname": "string (可选)",
  "role": "user | operator (必填)"
}
```

**业务逻辑**：
1. 验证用户名是否已存在
2. 验证密码强度（至少8位，包含字母和数字）
3. bcrypt加密密码
4. 创建用户记录
5. 如果是operator角色，创建待审核的租户记录
6. 返回成功信息

**响应示例**：
```json
{
  "code": 200,
  "message": "注册成功",
  "data": {
    "userId": 1,
    "username": "testuser",
    "role": "user"
  }
}
```

#### 任务5：用户登录
**接口**：`POST /api/v1/auth/login`

**请求参数**：
```json
{
  "username": "string (必填)",
  "password": "string (必填)",
  "rememberMe": "boolean (可选, 默认false)"
}
```

**业务逻辑**：
1. 查询用户是否存在且未删除
2. 检查账号是否被锁定
3. 验证密码
4. 如果失败，增加失败次数，达到5次锁定30分钟
5. 如果成功，重置失败次数，更新最后登录时间
6. 生成JWT Token
7. 返回Token和用户信息

**响应示例**：
```json
{
  "code": 200,
  "message": "登录成功",
  "data": {
    "token": "eyJhbGciOiJIUzI1NiIs...",
    "expiresIn": 7200,
    "user": {
      "id": 1,
      "username": "testuser",
      "nickname": "测试用户",
      "role": "user"
    }
  }
}
```

#### 任务6：Token刷新
**接口**：`POST /api/v1/auth/refresh`

**业务逻辑**：
1. 验证旧Token有效性
2. 生成新Token
3. 返回新Token

### 4.3 权限控制

#### 任务7：认证中间件
- [ ] 实现JWT验证中间件
- [ ] 从Token中提取用户信息
- [ ] 将用户信息注入到request对象
- [ ] 处理Token过期和无效情况

#### 任务8：权限中间件
- [ ] 实现角色权限检查
- [ ] 实现租户隔离检查
- [ ] 提供权限装饰器/守卫

### 4.4 管理员功能

#### 任务9：审核运营方
**接口**：
- `GET /api/v1/admin/tenants/pending` - 获取待审核列表
- `POST /api/v1/admin/tenants/:id/approve` - 通过审核
- `POST /api/v1/admin/tenants/:id/reject` - 拒绝审核

**业务逻辑**：
1. 查询所有status=pending的租户
2. 审核通过：更新租户status为approved，激活用户
3. 审核拒绝：更新租户status为rejected，记录拒绝原因

#### 任务10：用户管理
**接口**：
- `GET /api/v1/admin/users` - 获取用户列表（支持分页、筛选）
- `GET /api/v1/admin/users/:id` - 获取用户详情
- `POST /api/v1/admin/users/:id/reset-password` - 重置密码
- `PUT /api/v1/admin/users/:id/status` - 禁用/启用用户

**重置密码逻辑**：
1. 生成8位随机密码（包含字母和数字）
2. bcrypt加密后更新数据库
3. 返回明文密码给管理员（用于线下告知）
4. 记录操作日志

### 4.5 日志记录

#### 任务11：操作日志
- [ ] 创建日志服务
- [ ] 在关键操作中记录日志
- [ ] 异步写入日志，不影响主流程
- [ ] 实现日志查询接口

---

## 五、前端开发任务

### 5.1 基础配置

#### 任务1：项目初始化
- [ ] 使用Vite创建Vue/React项目
- [ ] 安装依赖包（UI组件库、axios等）
- [ ] 配置路由
- [ ] 配置状态管理
- [ ] 配置axios拦截器

#### 任务2：封装HTTP请求
- [ ] 创建axios实例
- [ ] 配置请求拦截器（添加Token）
- [ ] 配置响应拦截器（统一错误处理）
- [ ] 封装常用请求方法

#### 任务3：布局组件
- [ ] 创建主布局组件（Layout）
- [ ] 创建侧边栏组件（Sidebar）
- [ ] 创建顶部导航组件（Header）
- [ ] 实现路由守卫（需要登录的页面）

### 5.2 认证页面

#### 任务4：登录页面
**功能点**：
- [ ] 用户名输入框
- [ ] 密码输入框（可切换显示/隐藏）
- [ ] 记住我复选框
- [ ] 登录按钮
- [ ] 表单验证
- [ ] 登录失败提示
- [ ] 登录成功后跳转到对应角色的首页

**页面路径**：`/login`

#### 任务5：注册页面
**功能点**：
- [ ] 角色选择（普通用户/运营方）
- [ ] 用户名输入框
- [ ] 密码输入框（显示强度提示）
- [ ] 确认密码输入框
- [ ] 昵称输入框（可选）
- [ ] 运营方需填写申请描述
- [ ] 表单验证
- [ ] 注册成功提示并跳转登录

**页面路径**：`/register`

### 5.3 管理员页面

#### 任务6：管理员Dashboard
**功能点**：
- [ ] 显示统计数据（待审核数量、用户总数等）
- [ ] 快捷入口

**页面路径**：`/admin/dashboard`

#### 任务7：运营方审核
**功能点**：
- [ ] 待审核列表表格
- [ ] 显示申请人信息、申请描述、申请时间
- [ ] 查看详情按钮
- [ ] 通过/拒绝操作按钮
- [ ] 拒绝时弹出输入框填写原因
- [ ] 已审核历史记录

**页面路径**：`/admin/tenant-audit`

#### 任务8：用户管理
**功能点**：
- [ ] 用户列表表格（支持分页）
- [ ] 按角色筛选
- [ ] 搜索功能
- [ ] 查看用户详情
- [ ] 重置密码按钮（显示生成的密码）
- [ ] 禁用/启用开关

**页面路径**：`/admin/user-manage`

### 5.4 状态管理

#### 任务9：用户状态
- [ ] 存储用户信息
- [ ] 存储Token
- [ ] 登录/登出action
- [ ] 权限判断方法

---

## 六、API接口清单

### 6.1 认证接口
| 方法 | 路径 | 说明 | 是否需要认证 |
|------|------|------|-------------|
| POST | /api/v1/auth/register | 用户注册 | 否 |
| POST | /api/v1/auth/login | 用户登录 | 否 |
| POST | /api/v1/auth/refresh | 刷新Token | 是 |
| POST | /api/v1/auth/logout | 退出登录 | 是 |

### 6.2 管理员接口
| 方法 | 路径 | 说明 | 权限要求 |
|------|------|------|---------|
| GET | /api/v1/admin/tenants/pending | 待审核列表 | admin |
| POST | /api/v1/admin/tenants/:id/approve | 通过审核 | admin |
| POST | /api/v1/admin/tenants/:id/reject | 拒绝审核 | admin |
| GET | /api/v1/admin/users | 用户列表 | admin |
| GET | /api/v1/admin/users/:id | 用户详情 | admin |
| POST | /api/v1/admin/users/:id/reset-password | 重置密码 | admin |
| PUT | /api/v1/admin/users/:id/status | 修改状态 | admin |

---

## 七、开发规范

### 7.1 代码规范
- 遵循ESLint规则
- 使用Prettier格式化代码
- 函数和变量使用驼峰命名
- 组件使用帕斯卡命名
- 常量使用大写加下划线

### 7.2 Git提交规范
```
feat: 新功能
fix: 修复bug
docs: 文档更新
style: 代码格式调整
refactor: 重构
test: 测试相关
chore: 构建过程或辅助工具的变动
```

### 7.3 分支管理
- `main`: 主分支，生产环境代码
- `develop`: 开发分支
- `feature/xxx`: 功能分支
- `hotfix/xxx`: 紧急修复分支

---

## 八、测试要点

### 8.1 后端测试
- [ ] 用户注册功能测试（正常、重复用户名、弱密码）
- [ ] 用户登录功能测试（正常、错误密码、账号锁定）
- [ ] Token生成和验证测试
- [ ] 权限控制测试
- [ ] 管理员审核功能测试
- [ ] 密码重置功能测试

### 8.2 前端测试
- [ ] 登录页面功能和验证
- [ ] 注册页面功能和验证
- [ ] 路由守卫测试
- [ ] Token存储和使用测试
- [ ] 管理员页面权限控制

---

## 九、交付物

### 9.1 代码
- [ ] 后端完整代码
- [ ] 前端PC端完整代码
- [ ] 数据库初始化脚本

### 9.2 文档
- [ ] API接口文档（可使用Swagger）
- [ ] 部署说明文档
- [ ] 环境变量配置说明

### 9.3 演示
- [ ] 本地运行演示
- [ ] 核心功能演示视频（可选）

---

## 十、注意事项

### 10.1 安全注意
1. 密码必须使用bcrypt加密，盐值轮数至少10
2. JWT密钥使用强随机字符串，存储在环境变量中
3. 所有API接口必须进行参数校验
4. 敏感操作必须记录日志
5. 防止SQL注入，使用ORM的参数化查询

### 10.2 性能注意
1. 数据库查询添加合适的索引
2. 日志写入使用异步方式
3. 前端路由懒加载
4. 图片等资源使用CDN（后续优化）

### 10.3 开发注意
1. 先完成后端API，再开发前端页面
2. 使用Postman或Apifox测试API
3. 前后端联调前先Mock数据
4. 及时提交Git，写好commit message

---

**下一阶段**：第二阶段 - 核心业务模块（商品管理、用户管理、积分管理）
