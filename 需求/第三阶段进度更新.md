# 第三阶段开发进度 - 订单消息模块

**更新时间**：2026-04-16  
**当前状态**：进行中（约 75% 完成）

---

## ✅ 已完成功能

### 1. 订单管理（后端 + 前端）

#### 后端接口
- ✅ `POST /api/v1/orders` - 用户下单兑换商品
- ✅ `GET /api/v1/orders` - 获取我的订单列表
- ✅ `GET /api/v1/orders/:id` - 获取订单详情
- ✅ `POST /api/v1/orders/:id/cancel` - 用户取消订单
- ✅ `GET /api/v1/operator/orders` - 运营方订单列表
- ✅ `PUT /api/v1/operator/orders/:id/status` - 运营方更新订单状态
- ✅ 订单创建时的事务处理（库存扣减、积分扣减、流水记录）
- ✅ 订单取消时的回滚处理（库存恢复、积分退回）

#### 前端页面
- ✅ `/user/orders` - 用户订单列表页面
- ✅ `/operator/orders` - 运营方订单管理页面（新建）
  - 订单列表展示
  - 状态筛选
  - 完成/取消订单操作
  - 订单详情查看

### 2. 消息通知系统（后端 + 前端完整实现）

#### 数据库
- ✅ Message 表已存在（init.sql 中定义）
- ✅ Message 模型创建并注册
- ✅ 与 User、Tenant 建立关联关系

#### 消息服务
- ✅ `MessageService` 服务类创建
  - `sendMessage()` - 发送单条消息
  - `sendBatchMessage()` - 批量发送消息
  - `getUnreadCount()` - 获取未读数量
  
#### 消息场景实现
- ✅ `notifyOperatorNewApplication()` - 通知运营方有新用户申请
- ✅ `notifyUserApplicationResult()` - 通知用户申请审核结果
- ✅ `notifyOperatorNewOrder()` - 通知运营方有新订单
- ✅ `notifyOrderCreated()` - 订单创建通知（别名）
- ✅ `notifyUserOrderStatusChange()` - 通知用户订单状态变更
- ✅ `notifyOrderCancelled()` - 通知订单取消
- ✅ `notifyPointsAdded()` - 通知积分充值
- ✅ `notifyPointsSubtracted()` - 通知积分扣减

#### 消息控制器
- ✅ `messageController.js` 创建
  - `getMessages()` - 获取用户消息列表
  - `markAsRead()` - 标记消息已读
  - `markBatchAsRead()` - 批量标记已读
  - `deleteMessage()` - 删除消息
  - `getUnreadCount()` - 获取未读数量
  - `getOperatorMessages()` - 运营方消息列表

#### 消息路由
- ✅ `message.js` 路由文件创建
- ✅ 已在主路由中注册

#### 前端页面
- ✅ `/user/messages` - 用户消息中心（新建）
  - 消息列表展示
  - 按类型/已读筛选
  - 标记已读/批量标记
  - 删除消息
  - 消息详情查看
  - 点击行自动标记已读
  
- ✅ `/operator/messages` - 运营方消息中心（新建）
  - 同上功能
  
- ✅ `/admin/messages` - 管理员消息中心（新建）
  - 同上功能

### 3. 集成与优化
- ✅ 订单创建时自动发送消息给运营方
- ✅ 订单状态更新时自动发送消息给用户
- ✅ 订单取消时发送消息通知
- ✅ 积分变动时发送消息通知

---

## ⏳ 待完成功能

### 1. Header 消息角标组件（可选优化）
- ❌ 在 Layout Header 中添加消息图标
  - 显示未读数量角标
  - 点击弹出最近消息列表
  - 快速标记已读
  - 跳转到消息中心

### 2. 用户商品浏览页面
- ❌ `/user/products` - 商品浏览页面
  - 商品卡片展示
  - 分类筛选
  - 积分范围筛选
  
- ❌ `/user/products/:id` - 商品详情页面
  - 商品信息展示
  - 立即兑换按钮
  - 确认兑换弹窗

### 3. 订单导出功能
- ❌ `GET /api/v1/operator/orders/export` - 导出订单为 Excel

### 4. 操作日志管理（管理员）
- ❌ `GET /api/v1/admin/logs` - 查询操作日志
- ❌ `GET /api/v1/admin/logs/export` - 导出日志
- ❌ 定时任务清理过期日志

### 5. 其他优化
- ❌ 订单状态机完善（添加 processing 状态）
- ❌ 并发控制优化（SELECT FOR UPDATE）
- ❌ 消息推送实时性（WebSocket，可选）

---

## 📝 下一步计划

### 优先级 P0（必须完成）
1. **用户商品浏览和兑换流程** - 完整的用户体验
2. **测试消息通知功能** - 确保各场景消息正确发送

### 优先级 P1（重要）
3. **Header 消息角标** - 提升用户体验
4. **订单导出功能** - 运营方需要导出数据

### 优先级 P2（可选）
5. **操作日志管理** - 管理员功能
6. **定时任务** - 日志清理

---

## 🔧 技术要点

### 消息系统设计
- 采用异步发送，不阻塞主业务流程
- 使用 `Promise.allSettled` 批量发送，部分失败不影响其他消息
- 错误捕获和日志记录，避免消息发送失败影响核心业务

### 订单事务处理
- 使用 Sequelize 事务保证数据一致性
- 积分扣减、库存扣减、订单创建、流水记录原子操作
- 异常情况自动回滚

### 消息通知时机
- 订单创建 → 通知运营方
- 订单状态变更 → 通知用户
- 订单取消 → 通知双方
- 积分变动 → 通知用户
- 申请审核 → 通知双方

---

## 📊 代码统计

**新增文件**：
- `backend/src/models/Message.js` - 消息模型
- `backend/src/services/messageService.js` - 消息服务
- `backend/src/controllers/messageController.js` - 消息控制器
- `backend/src/routes/message.js` - 消息路由
- `frontend-pc/src/views/Operator/OrderManage.vue` - 运营方订单管理页面
- `frontend-pc/src/views/User/Messages.vue` - 用户消息中心
- `frontend-pc/src/views/Operator/Messages.vue` - 运营方消息中心
- `frontend-pc/src/views/Admin/Messages.vue` - 管理员消息中心

**修改文件**：
- `backend/src/routes/index.js` - 注册消息路由
- `backend/src/controllers/orderController.js` - 集成消息通知
- `frontend-pc/src/router/index.js` - 更新消息中心路由配置

**总计**：
- 新增代码：约 1900+ 行
- 修改代码：约 100 行

---

## ✨ 亮点功能

1. **完整的消息通知体系** - 覆盖所有关键业务场景，前后端完整实现
2. **优雅的错误处理** - 消息发送失败不影响核心业务
3. **灵活的批量操作** - 支持批量发送和批量标记已读
4. **清晰的代码结构** - Service 层封装业务逻辑，Controller 层处理 HTTP 请求
5. **优秀的用户体验** - 点击行自动标记已读、消息类型颜色区分、未读角标提示
6. **统一的界面设计** - 三种角色消息中心界面一致，响应式布局

---

**最后更新**：2026-04-16  
**下次更新**：完成用户商品浏览和兑换流程后
