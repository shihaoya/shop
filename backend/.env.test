# 测试环境配置
NODE_ENV=test

# 服务器配置
PORT=8367

# 数据库配置（使用测试数据库）
DB_HOST=localhost
DB_PORT=3306
DB_NAME=point_exchange_test
DB_USER=root
DB_PASSWORD=root

# JWT配置
JWT_SECRET=test_jwt_secret_key_for_unit_testing_only
JWT_EXPIRES_IN=1h
JWT_REFRESH_EXPIRES_IN=7d

# 日志配置
LOG_LEVEL=error
